<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :startStation %><br>
    <%= f.text_field :startStation %>
  </div>
  <div class="field">
    <%= f.label :endStation %><br>
    <%= f.text_field :endStation %>
  </div>
  <div class="field">
    <%= f.label :all %><br>
    <%= f.text_field :all %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
